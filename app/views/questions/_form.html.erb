<%= form_with model: [@test, @question], local: true, data: { turbo: false } do |form| %>
  <%= render "shared/errors", resource: @question %>

  <p>
    <%= form.label :body %>
    <%= form.text_field :body %>
  </p>

  <p><%= form.submit %></p>
<% end %>
